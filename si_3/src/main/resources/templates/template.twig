<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Mentor</title>
        <link rel="stylesheet" type="text/css" href="css/style.css" media="screen">
        <link href="https://fonts.googleapis.com/css?family=Indie+Flower|Pacifico" rel="stylesheet">
    </head>
    <body>
        <header>
            <h1>Guest-Book</h1>
        </header>
        <div id="addbutton" class="addbutton animate">
            <button onclick="createForm()" id="addstudent">Add Post</button>
        </div>
        <div id="studentsform">
            <form id="form_tag" method="POST">
                <div id="form"></div>
                <div id="add"></div>
            </form>
        </div>
        <div class="postbox">
            {% for row in postData %}

                <div class="postrow">
                    <label>Message</label>
                    <div class="textbox"><p>{{row[0]}}</p></div>
                </div>
                <div class="postrow">
                    <label>Name</label>
                    <div class="textbox"><p>{{row[1]}}</p></div>
                </div>
                <div class="postrow">
                    <label>Email</label>
                    <div class="textbox"><p>{{row[2]}}</p></div>
                </div>
                <div class="postrow">
                    <label>Date</label>
                    <div class="textbox"><p>{{row[3]}}</p></div>
                </div>
            {% endfor %}
        </div>
    </body>
    <script src="js/messageform.js"></script>
</html>